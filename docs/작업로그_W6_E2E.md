# W6 작업로그 — 모션 시스템 v2 통합 실측 (E2E, 2026-08-27)

- 설계 정본: `docs/개선안_2026-08-27_브리프-스토리보드-모션v2.md` §3-3(모션 v2)·§3-4(게이트)
- 선행: W1 템플릿 v2(`docs/작업로그_W1_템플릿v2.md`) · W5 게이트 3종(`docs/작업로그_W5_게이트.md`)
- 작업 폴더: `결과물/2026-08-27_E2E_풀링013-도입부-v6/`
- 범위: 이 폴더 + 이 로그만. 템플릿·스크립트·design.md·SKILL·다른 결과물은 **손대지 않음**. 커밋 없음. `npx` 없음.
- 재료: 풀링013 원본(`결과물/2026-08-06_풀링013_클로드_일잘러_노하우/`)의 `narration.m4a`·`transcript.json`·
  `full.srt`를 복사(원본 불변). 실측 구간 0~63.2s.

---

## 1. 게이트 수치 (비포 = 풀링013 원본 산출물)

### ① `check_motion.py` — 정지 화면 회귀

| 슬라이드 | 구간 | 길이 | **애프터 정지 비율** | 판정 | 비포 대응 |
|---|---|---|---|---|---|
| S01 훅 말풍선 | 0.00~5.74 | 5.74s | **41.5%** | OK | — |
| S02 소문 카드 | 5.74~12.90 | 7.16s | **49.3%** | OK | — |
| S03 현실 반문(컷) | 12.90~19.44 | 6.54s | **52.5%** | WARN | — |
| S04 천차만별 바 | 19.44~26.04 | 6.60s | **57.4%** | WARN | — |
| S05 시키는 방법(넛지) | 26.04~31.60 | 5.56s | **58.8%** | OK | — |
| S06 실패담 | 31.60~36.70 | 5.10s | **53.2%** | OK | — |
| S07 카운터(컷) | 36.70~43.58 | 6.88s | **48.4%** | OK | — |
| S08 오늘의 약속 | 43.58~49.92 | 6.34s | **59.3%** | WARN | — |
| S09 BA | 49.92~63.20 | 13.28s | **71.7%** | WARN | — |

| 지표 | 비포(풀링013 411.8s·42슬라이드) | 애프터(63.2s·9프레임) |
|---|---|---|
| 판정 대상(>6s) | 35개 | 6개 |
| **평균 정지 비율** | **97.2%** | **56.4%** (9프레임 전체 54.7%) |
| FAIL / WARN / OK | **35 / 0 / 0** | **0 / 4 / 2** |
| 죽은 박자(양쪽 정지 컷) | 0건, 단 한쪽 정지 컷 **41/41 경계 전수** | **1건**(S08 43.58s 크로스페이드), 한쪽 정지 7건 |
| `.slide--cut` 경계 | 없음 | 12.90s·36.70s **둘 다 죽은 박자 아님 (목표 달성)** |

목표(평균 60% 미만 · 컷 경계 2곳 죽은 박자 아님) **둘 다 충족**.

### ② `hf_audit.py` — 레이아웃·명암비

| 지표 | 비포(42샘플) | 애프터(27샘플) |
|---|---|---|
| 레이아웃 findings | 47 (error **2** / warning 45) | 4 (**error 0** / warning 4) |
| 명암비 실패 | **58 / 327** | **0 / 43** |

남은 warning 4건 = `escaped_container — span.bubble-tail`(말풍선 꼬리, 의도된 형태).

### ③ `check_storyboard.py`

`FAIL 86 · WARN 0` → **`FAIL 1 · WARN 0`**. 남은 1건은 감독 검토 항목(§4).

### ④ `check_caption_safe.py`

9슬라이드 전수 콘텐츠 픽셀 **0.000%** — 통과(임계 0.30%).

---

## 2. 수정 이력

### 2-1. STORYBOARD.md — 표기 형식만 (창작 값·큐 시각 불변)

| # | 무엇 | 왜 | 해소된 검사 |
|---|---|---|---|
| 1 | 모든 `Scene N (a~b s)` 시각을 **절대 → 프레임 로컬**로 환산(해당 프레임 `오디오` 시작을 뺌) | 규격 §2-3·게이트 모두 로컬 초를 전제한다. 절대 시각으로 적히면 B가 통째로 FAIL(W5 로그 "한계" 1번에 예고된 함정) | B 대부분 + D 대부분 |
| 2 | 화면 표시 카피 15건의 인용부호 `"…"` → `「…」`(「요즘 소문」「클로드로 다 한다」「맡기기만 하면 뚝딱」「…라는 사람들」「같은 일도 시키는 법에 따라」「대충 시키면」「제대로 시키면」「'시키는 방법' 자체가」「저도 처음엔 막 썼습니다」「실무 사용 시간」「똑똑한 직원처럼 부리는 법이 보였다」「오늘 영상」「실무에서 진짜 성과내는」「이걸 모르면」「내 일을 대신하는 똑똑한 직원」) | 파서 `QUOTE_RE`가 따옴표 안을 전부 **나레이션 어절 큐**로 읽는다. 화면 카피는 transcript에 없으니 전부 FAIL. 낫표는 파서가 큐로 보지 않는다 | D 나머지 |
| 3 | 리빌 서술어 `등장` 명시 4곳(F07 Scene 2·3, F09 Scene 2·3) | `is_reveal()`이 홀드 키워드("정지 쉼표"/"정지 유지"/"홀드")만 있고 `REVEAL_HINT` 사전 단어가 없으면 리빌이 아니라고 본다. "카운터"는 사전의 "카운트"와 문자열이 달라 안 잡힌다 | C(프론트로딩 오판) |

문구·프레임 경계·어절 큐 시각·필드 값은 **한 글자도 바꾸지 않았다**.

### 2-2. presentation.html — 캡처 3회, 판정→수정 2라운드

**1차 캡처** — 평균 정지 68.9% · FAIL 2(S08 83.1, S09 81.1) · 죽은 박자 3건(**36.70s 컷 포함**).

프레임 차분을 직접 떠서 원인을 갈랐다(`d1` 30fps · `d5` 10fps):

| 원인 | 실측 근거 | 조치 |
|---|---|---|
| **CSS 지속 모션이 가상시간 캡처에서 접힌다** | `wipe-reveal`(clip-path 0.95s)이 1프레임에 끝남. `.slide--cut` 컷인(0.36s)이 36.70s에서 **2프레임(0.07s)** 만에 완료 → 컷 모션이 0.1s 격자 한 칸에 다 들어가 `죽은 박자` 판정. 반면 GSAP 타임라인(22.03~22.83, 38.30~39.80)은 1.5s를 그대로 그린다 | **큰 면적·긴 지속 모션을 전부 GSAP 로컬 타임라인으로 이관.** 9프레임 전부 `data-tl` 부여(규격 §2-3: Scene 2개 이상 = `data-tl`). 이음매 진입도 GSAP(`x 110→0`, power4.out 0.6s)이 소유 |
| **진폭 부족** | 리빌 시각은 정확한데 요소가 작아 `d5 < eps(1.0)`. 예: 12px 룰 바 최대 0.7, 44px 헤일로 0.5 | 캐리어 요소 확대(룰 바 30→96px, 헤일로 420→760px, 밑줄 14~20→44~48px), 이징 power3.out→power2.out(진폭을 지속시간에 고르게 분산) |
| **`.el--pop`은 `.el`과 같이 써야 발화** | 스티커·아이콘이 프레임 0부터 보임 | 팝을 GSAP `fromTo(scale/opacity/y)`로 교체 |
| **F04 뉴트럴 바가 트랙과 거의 같은 밝기** | `rgba(20,20,19,0.14)` vs 트랙 `#efeeea` → 델타 ~30, 진폭 0.57 | 뉴트럴 채움을 `--text-tertiary`로, 값 라벨은 잉크 |

**2차 캡처** — 평균 **55.4%** · FAIL 0 · **컷 경계 2곳 죽은 박자 없음**(목표 달성). 죽은 박자는 S08 크로스페이드 1건만 남음.

**HF 감사기 라운드**(2차 캡처 후) — error 5 / 명암비 실패 10:

| 검출 | 조치 |
|---|---|
| `text_occluded` — 조립 와이프 커버가 아직 안 걷힌 텍스트를 덮음(S08 타이틀, S09 ba-label) | HF 공식 예외 플래그 `data-layout-allow-occlusion`을 `.has-cover`/`.ba-card`에 부착(벤더 스크립트 `hasAllowOcclusionFlag`는 `closest()`로 조상까지 본다) |
| `text_occluded` — 강조 밑줄(44~48px)이 자기 텍스트 하단을 덮음(S06·S09) | 밑줄 `bottom`을 -20~-30px → -54~-64px로 내리고 그만큼 여백 확보 |
| **버그**: `.s08-title span { position: relative }`가 자손 선택자라 `.wipe-cover`의 `position:absolute`를 덮어썼다 → 커버가 흐름 요소가 되어 타이틀 와이프가 **아예 렌더되지 않음**(2차 캡처 S08 44.4~49.2s `d5`가 정확히 0.0) | `.s08-title > span` 자식 선택자로 스코프. 결과 코드에 경고 주석 남김 |
| 명암비 실패 10건 — `#F59E0B` 텍스트 2.15:1, `.section-label` `#D97706` on `#FEF5E6` 2.95:1, 앰버 바 위 흰 값 2.09:1, 그린 `#16a34a` 작은 텍스트 3.3:1 | 개선안 §2 방침대로 텍스트 색만 조정: 큰 숫자 → `--accent-text(#D97706)`, 작은 강조 → `#b06f07`, 작은 그린 → `#107a38`, 앰버 바 위 값 → 잉크. **그래픽 면(바·블록)의 앰버 원색은 유지** |
| `container_overflow` — 커버 `inset:-8px` | `inset:0` + `border-radius: inherit` |

**3차 캡처**(감사 수정 반영, HTML↔mp4 정합용) — 평균 **56.4%** · FAIL 0 · 컷 경계 2곳 정상 · HF error **0** · 명암비 실패 **0/43**.

캡처 횟수 총 3회(초기 1 + 모션 게이트 재캡처 2). 모션 게이트 재캡처 한도(2회) 내.

---

## 3. 구현 요약 (presentation.html)

- 템플릿 `presentation-16x9.html`(v2) 사본 + `벤더/gsap.min.js` 동봉 + 스티커 1종 복사. `lottie` 참조는 제거(미동봉).
- `SLIDE_TIMELINE` = STORYBOARD 오디오 필드 그대로 9경계(0 / 5.74 / 12.9 / 19.44 / 26.04 / 31.6 / 36.7 / 43.58 / 49.92).
- **어절 리빌**: `.el--w-anchor/-norm/-light`에 인라인 `style="transition-delay: <슬라이드 로컬 초>"`로 절대 지연 고정
  (스태거 수식 미사용). 로컬 초 = STORYBOARD 큐(transcript 실측 절대 시각) − 프레임 진입 시각.
- **지속 모션**(route 소유 구간): 슬라이드 로컬 GSAP `paused` 타임라인 9개. `fromTo`만 사용, 카운터·바 값은
  `tl.set` 프레임 행(30fps 격자). `tl.seek()` 미사용, `repeat:-1`·`Date.now`·`Math.random` 없음.
  - 조립 와이프(흰 커버 `scaleX 1→0`) · 성장 바(`scaleX 0→1`) · 드로우온(`strokeDashoffset 1→0`) ·
    카운터 프레임 행 · 헤일로 펄스(`repeat:2` 유한) · 이음매 진입(`x 110→0`)
- **전환 2종만**: 기본 크로스페이드 + `.slide--cut`(F03·F07 진입). `.nudge`는 F05.
- 스티커: `레퍼런스/스티커/말풍선-물음표-잉크.svg` 재사용(F01) + F06 "당황" 스티커는 4색 인라인 SVG 신규
  (파일로 등재하지 않음 — 이 결과물 안에서만 사용).
- 자막 안전영역 21vh 침범 0.000%. 한글 우선(영어 라벨 없음).

---

## 4. 감독 검토 항목 (STORYBOARD, 수정하지 않음)

**F07 Scene 3 어절 큐 `'똑똑한'`** — `[FAIL][D] 실측 40.54s가 창 41.00~43.60s 밖`.

스토리보드 원문이 `"똑똑한" 40.54 **반박자 뒤 41.1**`이라고 의도적으로 반박자를 뒀는데, 게이트 허용 오차가
±0.30s(→ 하한 40.70s)라 **0.16s** 모자란다. 창작 의도라 값을 고치지 않았다. 구현은 스토리보드대로 로컬 4.40s
(=41.10s)에 리빌한다. 선택지:

- ⓐ Scene 3 시작을 4.15s(=40.85s)로 당긴다 — 스토리보드 한 줄 수정, 게이트 통과
- ⓑ `check_storyboard.py`의 `CUE_TOL`을 0.5s로 완화 — "반박자 뒤" 연출을 규격이 허용하게 한다
- ⓒ 현행 유지 — 이 한 건은 사람 판정으로 넘긴다

부수 제안 1건: **F09가 13.28s로 혼자 길고**(정지 71.7%, 유일한 WARN 다수 기여) Scene 2(4.98~10.98s)에
어절 큐가 8.88s 하나뿐이라 6.9~8.9s를 채울 소재가 없다. 프레임을 둘로 쪼개거나 Scene 2에 중간 큐 1개 추가 권고.

---

## 5. 검증 프레임 (20장)

`C:\Workspace-Hub\공유 프로젝트\youtube-creator\결과물\2026-08-27_E2E_풀링013-도입부-v6\검증프레임\`

`f0006_t0.2` · `f0081_t2.7` · `f0120_t4` · `f0207_t6.9` · `f0276_t9.2` · `f0418_t13.95`(컷 직후) ·
`f0534_t17.8` · `f0567_t18.9` · `f0675_t22.5` · `f0750_t25` · `f0888_t29.6` · `f0912_t30.4` ·
`f1020_t34` · `f1106_t36.85`(컷 직후) · `f1185_t39.5` · `f1245_t41.5` · `f1416_t47.2` ·
`f1590_t53` · `f1776_t59.2` · `f1860_t62`

(`-ss` 앞자리 시크가 같은 프레임을 반복 반환해 `select=eq(n,N)` 프레임 번호 지정으로 추출)

---

## 6. 남은 일 / 인계

1. **템플릿 v2 후속 과제(중요)** — CSS `transition`/`animation` 기반 지속 모션은 가상시간 캡처에서
   1~2프레임으로 접히는 경우가 있다(이번 실측으로 확정). 템플릿의 `.slide--cut`·`wipe-reveal`·
   `hbar-fill` 같은 CSS 지속 모션 preset은 **믿을 수 없다**. 두 갈래 중 택일 필요:
   ⓐ 렉시콘 지속 preset을 GSAP 구현으로 재정의 ⓑ `capture_slides.py`에서 CSS 애니메이션이 가상시간을
   따르도록 조사·수정. 이번 결과물은 ⓐ를 우회적으로 실천한 것(모든 지속 모션을 GSAP이 소유).
2. **진폭 가이드 부재** — `check_motion.py`의 `eps=1.0 @320px`를 넘으려면 요소가 얼마나 커야 하는지
   설계 문서에 수치가 없다. 실측 경험칙: 풀폭(1420px) 바는 높이 **50px 이상**, 카드 조립 와이프는
   **1.5s 이하**, 텍스트는 `--text-2xl` 이상이어야 0.5s 격자에서 잡힌다. design.md §4 등재 검토 대상.
3. **`check_storyboard.py` 개선 후보** — ⓐ 화면 카피와 나레이션 큐를 표기로 구분하는 규약을 규격 §2-3에
   명문화(이번엔 `「」`를 썼다) ⓑ `REVEAL_HINT`에 "카운터/와이프/조립/성장" 추가 ⓒ Scene 시각이 절대로
   적힌 경우를 감지해 "로컬 초로 적으세요" 힌트 출력(이번 86 FAIL 중 85건이 이 두 함정).
4. **S08 크로스페이드 죽은 박자 1건** 미해결(임계 0.157 vs eps 1.0). 나가는 슬라이드 끝에 잔여 모션을
   두거나 이 경계도 컷으로 바꾸면 해소되나, 전환 예산(2종)과 스토리보드 지정을 건드려야 한다.
5. **미수행** — 편집지시서·SRT 분할·CapCut 조립·`validate_pipeline.py`(모션 검증 목적이라 범위 밖).
6. **오너 검수** — 20장 프레임 육안 확인, F09 프레임 분할 여부, §4 선택지 ⓐ/ⓑ/ⓒ 결정.

---

## 7. v6.1 절제 패스 (2026-08-27 오너 피드백 반영)

오너 판정: ①F02 주황 바(96px)가 카드 본문을 가림 ②F04 바 안 숫자가 찌그러짐 ③전반적으로 주황 밑줄이
과하고 인포그래픽이 과장됨. **근본 원인 = §3 "진폭 부족" 대응에서 게이트(eps)를 넘기려 장식을 44~96px로
키운 것.** 게이트 체이싱이 룩을 망쳤다 — design.md §4에 금지 규칙 3종으로 등재.

수정(결과물 presentation.html):
- 알약 밑줄 7종(foot/s06/s07/s08-sub/s09-label/s09-cta + s05는 hl-sweep와 중복이라 제거) → **형광 마커**
  (텍스트 하단 절반, `0.52em`·앰버 `opacity 0.38`·`mix-blend-mode:multiply`, s09-label은 텍스트 폭 4.6em만)
- `.rumor-bar` 96→12px, `.s08-rule` 96→10px, F04 트랙 6.6→3.8rem, F07 트랙 4→1.4rem
- **F04 `.tlbar` scaleX→width 트윈**(값 라벨 왜곡 해소, `overflow:hidden`)

재실측(3차 캡처): FAIL 0 · 죽은 박자 1(S08 크로스페이드, 기존 잔존) · HF 감사 error 0 · 명암비 실패 0/46 ·
검증프레임v2/ 11장 육안 PASS. **평균 정지 비율 54.7→65.4%(WARN 6)** — 장식 진폭을 걷어낸 정직한 수치.
정지 비율을 더 낮추려면 장식이 아니라 소재(콜라주 인서트·시연 오버레이·F09 분할)로 채워야 한다.

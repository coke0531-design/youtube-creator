# 작업로그 W4 — 문서 계층 개정 (2026-08-27)

설계 정본 = `docs/개선안_2026-08-27_브리프-스토리보드-모션v2.md` §3-0·3-1·3-2·3-3·3-4·3-5.
이 작업은 **새 설계 없음 — 개선안 내용을 규범 문서로 번역·편입**만 했다. 모든 수정은 Edit 증분(통째 재작성 없음). 커밋하지 않음.
수정 금지 영역(`템플릿/**`·`scripts/**`·`벤더/**`)은 건드리지 않았다.

## 1. 파일별 전후 줄수

| 파일 | 전 | 후 | 증감 |
|------|----|----|------|
| `design.md` | 246 | 271 | +25 |
| `CLAUDE.md` | 79 | 85 | +6 |
| `.claude/skills/youtube-editor/SKILL.md` | 529 | 619 | +90 |
| `.claude/skills/youtube-editor/스토리보드-규격.md` | (없음) | 187 | 신설 |

## 2. 추가 소절 목록

### 2-1. `design.md` §4 (모션 토큰)

| 위치 | 추가 내용 | 출처 |
|------|------|------|
| 렉시콘 표 (line 159~162) | preset 4행: **스태거--무게**(`.el--w-anchor`/`.el--w-norm`/`.el--w-light`) · **넛지 커브**(`.nudge`) · **컷 더 커브**(`.slide--cut`) · **샷 시퀀스**(`data-tl` + 로컬 GSAP paused 타임라인) | §3-3 |
| 표 아래 불릿 (line 169) | 샷 시퀀스 구현 제약 3가지 — `fromTo`만 / 무한 `repeat` 금지 / `tl.seek()` 금지·`totalTime` 2호출, GSAP `<script>` 선행 로드 | §2 핵심 함정 + §3-3 |
| 신설 소절 (line 171~177) | **모션 교리 4법칙** — ①부드러움>튐(power3 롱테일, bounce/elastic 금지, back.out 1.4~1.7 유지) ②리빌은 나레이션 큐·후반 50%, 프론트로딩 FAIL ③나쁜 모션보다 정지 + 루트 5종 + 클라이맥스 앞 0.3~0.75s 쉼표 ④컷은 속도 일치(미러 이징 power4.in/out) | §3-3 |
| 신설 소절 (line 178~188) | **벡터 예약표** — LEFT 기본 조류 / 위쪽 = 파트 결론·고양 / Z 후퇴(줌 스루·인버스 줌, blur 10px·18~20px) = 파트 타이틀 도착 + 연속 역방향 금지 + 전환 예산 2~3종 | §3-3 |
| 출처 한 줄 (line 189) | HyperFrames v0.8.16 (HeyGen, Apache-2.0) 개작 흡수 + 결정 기록 문서 포인터 | §0·§4-6 |

**기존 규칙 보존 확인**: 콜라주 게이트("전 구간 콜라주 금지" 2026-08-17 / "오프닝(t=0) 필수" 2026-08-18), 레드 금지, 화이트보드 비활성, 4색 원칙, 강조 preset 슬라이드당 1종 — 삭제·약화 없음.

### 2-2. `.claude/skills/youtube-editor/스토리보드-규격.md` (신설 187줄)

| 절 | 내용 | 출처 |
|------|------|------|
| 머리말 | 두 파일(BRIEF/STORYBOARD)의 위치·단계·역할 표, 상·하류 계약 | §3-0·3-1 |
| §1-1 | BRIEF 프론트매터 8키(`message`/`audience`/`goal`/`angle`(4종 enum)/`length`/`tone`/`visual_concept`/`must_show`/`avoid`) | §3-0 |
| §1-2 | 본문 3절 (`## 의도` / `## 재료` / `## 메모`) | §3-0 |
| §1-3 | 채움 절차 3단계 + **피치 3~5안 규칙**(최소 2안 비관습 경로, 5안 전부 제시 후 추천) + 오너 확인/자율 폴백 | §3-0 |
| §2-1 | `## Video direction` 블록 1회 (팔레트 4색·모션 문법·조류/예약 벡터·전환 예산·정지 배분·금지 목록) | §3-1 |
| §2-2 | 프레임 필드 8종 전부 — `type`(8역할 enum) `persuasion` `beat` `blueprint` `route`(5종) `vec` `focal` `오디오` | §3-1 |
| §2-3 | `Scene` 줄 문법 — 단어 타임스탬프 실측, 창 개수 = 나레이션 큐 개수, 프론트로딩 금지·후반 50% 리빌, 정지 홀드 명시, Scene ≥2 → `data-tl` | §3-1·3-3 |
| §2-4 | **실전 예시 2프레임** (Frame 07 두더지 잡기 / Frame 08 수치 결론) | §3-1 예시 확장 |
| §2-5 | `check_storyboard.py` 게이트 5항목 | §3-4 |
| §3 | 워커 분업 규약 — 6프레임 이하 인라인 / 패킷 3구성(프레임 블록+블루프린트+rule 본문, 48KB) / **워커는 STORYBOARD 전체 열람 금지** / 출력 = `<section class="slide">`+로컬 타임라인 / 감독의 조립·벡터 연속성 검수 / 모델 규칙 | §3-2 |

### 2-3. `.claude/skills/youtube-editor/SKILL.md`

| 위치 | 변경 | 출처 |
|------|------|------|
| 산출물 표 | ⑦ `01_대본/BRIEF.md` · ⑧ `04_영상소스/STORYBOARD.md` 행 추가 | §3-0·3-1 |
| 참조 파일 표 | `스토리보드-규격.md` 행 추가 | — |
| 워크플로우 개요도 | `0.5 기획 브리프` · `4. 스토리보드 작성` · `4.5 스토리보드 게이트` · `5. 소스 제작`에 분업/템플릿 v2 · `6. 동기화 데이터`에 스토리보드 상류 · `6.8 HF 히어로 인서트` · `7.5 모션 게이트` 반영 | §3-0~3-5 |
| **Step 0.5 (신설)** | 기획 브리프 — 규격 파일 포인터 + 3단계(자동 추론 → 피치 3~5안 → 확인 게이트/자율 폴백) + no-repeat token 규칙 | §3-0 |
| **Step 4 (개편)** | "슬라이드 분해" → **"스토리보드 작성"**. 구 분해 규칙표를 **프레임 분해 규칙**으로 흡수(+ `focal` 1개·`route` 필수·샷 창 = 나레이션 큐 개수 행 추가). STORYBOARD.md 산출 + **Step 4.5 `check_storyboard.py` 게이트**(통과 전 Step 5 금지) | §3-1·3-4 |
| **Step 5 (개편)** | 제목 "프레임 제작 (분업)". 1항에 **템플릿 v2 계약**(`data-tl` 사용 시 `벤더/gsap.min.js` 동반 복사·CDN 금지·GSAP 스크립트 선행), 2항에 STORYBOARD 순서 준수. 말미에 **분업 절차 블록**(6프레임 이하 인라인 / 워커 1명=2~3프레임 / 패킷 48KB / 전체 열람 금지 / 출력 계약 / 감독 조립·벡터 연속성 / 모델 규칙) | §3-2·3-3 |
| 타임라인.json `style` 설명 | `"hyperframes"` 값 추가(render_hf.py, encode_overlays 건너뜀, 25% 합산) | §3-5 |
| **Step 6.8 (신설)** | HF 히어로 인서트 — 편당 0~2개 + 콜라주 합산 ≤25%, 스캐폴드(`04_영상소스/hf/<n>/`·핀 버전·로컬 GSAP·Pretendard `@font-face url()`·`data-duration` 정적), 명암비 임계 3:1/4.5:1 불변, `lint→check→snapshot` 선행, `render_hf.py` 명령·`npx.cmd`, 무음 렌더, `HYPERFRAMES_SKIP_SKILLS=1 HYPERFRAMES_NO_TELEMETRY=1`·`.env` 함정 | §3-5·§2 |
| **Step 7.5 (신설)** | 모션 게이트 — `check_motion.py`(1초 격자 프레임 차분·컷 경계 4프레임 죽은 박자) + 선택 `hf_audit.py`(layout/contrast/motion 주입·한글 라벨 8자 재보정). 실패 시 **스토리보드로 회귀** | §3-4 |
| 체크리스트 산출물 | BRIEF.md · STORYBOARD.md · `check_storyboard.py` 통과 · 분업 수행 · `check_motion.py` 통과 · HF 인서트 5줄 추가 | §3-0~3-5 |
| 체크리스트 디자인 | 모션 교리 4법칙 · 벡터 예약표 2줄 추가 | §3-3 |

### 2-4. `CLAUDE.md` (최소 분량 — 상세는 포인터)

| 절 | 변경 |
|------|------|
| 디자인 SSOT | 모션 규범도 design.md SSOT(교리 4법칙·벡터 예약표) 1줄 + **`docs/개선안_2026-08-27_…md` = 이번 개편의 결정 기록** 1줄 |
| 스킬 파이프라인 표 | `0. 의도·연출 계층` 행 신설 (BRIEF → STORYBOARD, 규격 파일 포인터) |
| 입력 계약 | "대본+녹음뿐이라 의도가 빈다 → BRIEF.md가 메운다, 어떻게 보여줄까만 다룸, no-repeat token" 문장 추가 |
| 싱크 계약 | `2-1.` 항목 신설 — 스토리보드가 상류 / 단어 타임스탬프 실측 / `data-tl`+로컬 GSAP·`totalTime` 2호출·`tl.seek()` 금지 / 모션 규범 = design.md §4 |
| 싱크 계약 | `7.` 항목 신설 — **게이트 2종**(`check_storyboard.py` Step 4 직후 / `check_motion.py` Step 7 캡처 직후) + 선택 `hf_audit.py` + **기존 게이트 유지 명시**(validate_pipeline·check_caption_safe 21vh·split_captions 16자) + HF 인서트 25% 합산 |
| 폴더 규약 | `01_대본/`에 BRIEF.md, `04_영상소스/`에 STORYBOARD.md 표기 |
| 환경 전제 scripts 목록 | `check_storyboard.py`·`check_motion.py`·`hf_audit.py`·`render_hf.py` + `벤더/gsap.min.js` 추가 |

## 3. 정합 대조표 (개선안 ↔ 개정 문서, grep 전수)

문자열 일치 = `grep -F` 완전 일치 기준.

| 토큰 | 개선안 | 반영 파일 | 판정 |
|------|------|------|------|
| `check_storyboard.py` | ✓ | design.md · CLAUDE.md · SKILL.md · 스토리보드-규격.md | 일치 |
| `check_motion.py` | ✓ | CLAUDE.md · SKILL.md · 스토리보드-규격.md | 일치 |
| `render_hf.py` | ✓ | CLAUDE.md · SKILL.md | 일치 |
| `hf_audit.py` | ✗ (개선안은 "HF 감사기 주입"으로만 기술, 스크립트명 미기재) | CLAUDE.md · SKILL.md | **작업 지시서가 명명한 이름을 채택** — 개선안과 불일치 아님(미기재 항목) |
| `벤더/gsap.min.js` | ✓ (실물 존재 확인) | design.md · CLAUDE.md · SKILL.md | 일치 |
| `data-tl` | ✓ | design.md · CLAUDE.md · SKILL.md · 스토리보드-규격.md | 일치 |
| `.slide--cut` | ✓ | design.md | 일치 |
| `.el--w-anchor` / `.el--w-norm` / `.el--w-light` / `.nudge` | ✗ (개선안은 수치만, 클래스명 미기재) | design.md | **작업 지시서가 명명한 클래스명 채택** — 수치(60~80px·0.16~0.20s / 40~50px·0.13~0.16s / 30~48px·0.10~0.13s, 10/65/25·20/18/62)는 개선안과 일치 |
| `style:"hyperframes"` | ✓ | CLAUDE.md · SKILL.md | 일치 |
| 루트 5종 `staged-reveals`·`camera-with-intent`·`sequenced-ui-life`·`animated-sequences`·`carrier-led` | ✓ | design.md · 스토리보드-규격.md | 5/5 일치 |
| 8역할 enum `hook`·`pain_point`·`product_intro`·`feature_showcase`·`benefit_highlight`·`social_proof`·`branding`·`cta` | ✓ | 스토리보드-규격.md | 8/8 일치 |
| 블루프린트 부품 10종(`count-up`·`chart-story`·`grid-card-assemble`·`state-chip-rail`·`before-after-wipe`·`marker-highlight`·`vox-annotate`·`per-word-rise`·`titlecard-lockup`·`cta-close`) | ✓ | 스토리보드-규격.md | 10/10 일치 |
| `totalTime` 2호출 / `tl.seek()` 금지 | ✓ | design.md · CLAUDE.md | 일치 |
| `power3.in` / `power4.in` / `power4.out` | ✓ | design.md | 일치 |
| `BRIEF.md` / `STORYBOARD.md` 경로 | ✓ (`01_대본/BRIEF.md`, Step 4 산출) | CLAUDE.md · SKILL.md · 스토리보드-규격.md | 일치 |
| 수치: 12%·230px / 25~30% / 0.35 점화 / 0.3s | ✓ | design.md | 일치 |
| 수치: 0.3~0.75s 정지 쉼표 | ✓ | design.md · SKILL.md · 스토리보드-규격.md | 3파일 동일 |
| 수치: 전환 유형 2~3종 (4종 이상 FAIL) | ✓ | design.md · SKILL.md · 스토리보드-규격.md | 3파일 동일 |
| 수치: 패킷 48KB / 워커 2~3프레임 / 6프레임 이하 인라인 | ✓ | SKILL.md · 스토리보드-규격.md | 일치 |
| 명암비 3:1 · 4.5:1 (변경 불가) | ✓ | SKILL.md | 일치 |
| HF 버전 `hyperframes@0.8.16` / Apache-2.0 | ✓ | SKILL.md(명령) · design.md(출처 줄) | 일치 |

### 3-1. design.md ↔ SKILL.md 모순 점검 (색·게이트·수치)

| 항목 | design.md | SKILL.md | 판정 |
|------|------|------|------|
| 4색 (`#ffffff`/`#141413`/`#F59E0B`/`#D97706`/`#16a34a`) | 정의 | 동일 값 인용 | 모순 없음 |
| `#b06f07` | **미등재** | Step 6.8에 "검토 중, **미채택** — 추가하려면 design.md에 먼저 등재" 로 기재 | 모순 없음(SSOT 우선 명시) |
| 자막 안전영역 21vh / 230px | 230px 표기 | 21vh·230px 병기(기존 문구 그대로) | 모순 없음 |
| ⚠ `230px` 중복 의미 | 컷 더 커브의 "부분 이동 12%(1920에서 230px)" | 자막 안전영역 230px | 문맥이 달라 규칙 충돌은 없으나 **같은 숫자가 두 의미로 존재**(개선안 원문 표기를 그대로 옮김) |
| 오버레이 총량 ≤25% | 콜라주 행에 기재 | 콜라주(6.7)·HF(6.8) **합산** 명시 | 모순 없음(예산 통합) |
| 전환 예산 2~3종 | 벡터 예약표 | Step 4.5 게이트·체크리스트 | 동일 |
| `.pulse` 반복 | 렉시콘 행 "2s 반복" + 신설 불릿 "무한 repeat 금지, `.pulse`도 유한 반복" | — | 모순 없음(주기 vs 무한 여부) |
| 크로스 디졸브 | 기본값 행 유지 + 컷 더 커브 행에 "운반체 없는 경계에만" | — | 약화 아님(옵트인 병존) |

## 4. 이번 문서 개정에 **넣지 않은** 개선안 항목 (다른 워커/후속 담당)

- §3-3 "기존 수정 2건"의 **구현**(`.pulse` 유한화, `go(0)` 캡처 모드 처리) — `템플릿/**` 담당.
- §3-4의 **caption-safe 샘플 0.5초 격자 확대** — `scripts/check_caption_safe.py` 변경 사항이라 `scripts/**` 담당. 문서에는 기존 규칙(21vh·구간당 검사)을 그대로 유지했다.
- §3-6 **콜라주 룩 v2**(`grid-cream`·redTab·highlight 등) — 작업 지시 범위 밖(별도 워커).
- §4-6 **ATTRIBUTIONS 등재**(HyperFrames Apache-2.0 / GSAP / kez·gasket) — 전역 규칙상 세션 단위 기록이며 이 워커 범위 밖. design.md에 출처 한 줄은 반영함.

## 5. 남은 확인

- ⚠️ `check_storyboard.py`·`check_motion.py`·`hf_audit.py`·`render_hf.py`는 **아직 실물 없음**(신설 예정 — 개선안 §4 구현 순서 2·3단계). 문서는 이 이름을 계약으로 선언한 상태다.
- ⚠️ 오너 검수 0건, 실전 적용 0건(풀링013 실측은 개선안 §4-5 단계).
- 커밋하지 않음.

# PULLING 브랜드 디자인 시스템 (design.md)

**이 파일은 모든 시각 산출물의 단일 출처(SSOT)다.**
영상 소스 슬라이드(16:9 / 9:16), 인포그래픽, 썸네일, 기타 그래픽을 만들 때 색상·폰트·모션을 하드코딩하기 전에 반드시 이 파일을 따른다. 스킬 문서와 이 파일이 충돌하면 **이 파일이 우선**한다.

> **원본 레퍼런스** (브랜드 톤의 출처 — 새 컴포넌트가 필요하면 여기서 패턴을 가져온다):
> - `D:\Workspace\MY_Ian\2. Area\영상 모음\풀링_006\영상 소스\풀링-006-presentation.html`
> - `D:\Workspace\MY_Ian\2. Area\영상 모음\풀링_005\영상 소스\풀링-005-presentation.html`
> - `D:\Workspace\MY_Ian\2. Area\영상 모음\풀링_004\영상 소스\풀링-004-presentation.html`
>
> **즉시 사용 가능한 보일러플레이트**: `템플릿/presentation-16x9.html`, `템플릿/presentation-9x16.html`

---

## 1. 브랜드 아이덴티티

| 항목 | 값 |
|------|------|
| 채널/시리즈 표기 | `PULLING · EP.NNN` (섹션 라벨), 풀링 |
| 무드 | 따뜻한 미니멀. 흰 바탕, 절제된 강조, 넉넉한 여백, 정돈된 카드 |
| 강조 철학 | **주 강조 = 앰버, 보조 강조 = 그린.** 그 이상 색을 늘리지 않는다 |
| 금지 무드 | 무지개색, 보라 그라데이션, 네온 사이버펑크, 어두운 대시보드 느낌 |

**핵심 원칙 — 4색 원칙**: 모든 영상 소스/인포그래픽은 「**흰 배경 + 잉크 + 앰버(주 강조) + 그린(보조 강조)**」 구조를 유지한다. 뉴트럴 음영(표면/보더/보조 텍스트)은 배경·잉크의 변형으로 간주하며 색 수에 세지 않는다.

---

## 2. 컬러 토큰

### 2-1. 기본 팔레트 (라이트 — 영상 소스·프레젠테이션 공통 기본)

| 토큰 | 값 | 용도 |
|------|------|------|
| `--bg-page` | `#ffffff` | 페이지/슬라이드 배경 (**영상 소스는 흰색 고정**) |
| `--bg-surface` | `#f7f6f4` | 카드/표면 |
| `--bg-elevated` | `#efeeea` | 한 단계 위 표면, 차트 빈 트랙 |
| `--bg-border` | `rgba(20,20,19,0.08)` | 기본 보더 |
| `--bg-border-strong` | `rgba(20,20,19,0.14)` | 강한 보더 |
| `--text-primary` (잉크) | `#141413` | 제목/본문/기본 도형 |
| `--text-secondary` | `#5e5d59` | 부제/설명/보조 라벨 |
| `--text-tertiary` | `#a39e98` | 메타/캡션 한정 (중요 정보 금지) |
| `--accent` | `#F59E0B` | **주 강조 (앰버)** — 그래픽/큰 숫자/도형 |
| `--accent-deep` | `#D97706` | 주 강조의 **작은 텍스트용** 변형 (가독성) |
| `--accent-light` | `#FBBF24` | 앰버 밝은 변형 (다크 배경 위에서만) |
| `--accent-subtle` | `rgba(245,158,11,0.10)` | 강조 배경 채움 (칩/라벨 배경) |
| `--accent-ring` | `rgba(245,158,11,0.25)` | 강조 링/포커스 |
| `--green` | `#16a34a` | **보조 강조 (그린)** — 성공/체크/2번째 데이터 시리즈 |
| `--green-soft` | `rgba(22,163,74,0.08)` | 그린 배경 채움 (After 카드 등) |
| `--green-ring` | `rgba(22,163,74,0.22)` | 그린 링/보더 |

> **보조 강조 선정 근거**: 풀링 004~006 세 자료 모두에서 앰버 다음으로 일관되게 쓰인 의미 색이 success 그린(✓ 체크 불릿, After 카드, num--success)이다. error 레드는 부정 전용이라 범용 보조 강조로 부적합, info 블루는 006에만 존재 → 그린 채택.

### 2-2. 다크 모드 토큰 (프레젠테이션 토글 전용 — 영상 소스에는 사용 금지)

원본 프레젠테이션의 다크 토글용으로만 보존한다: bg `#0a0a0f`/`#151518`/`#1c1c22`, 텍스트 `#eaeaef`/`#9a9ab0`/`#6b6b80`, 앰버 `#FBBF24`/`#FCD34D`/`#F59E0B`, 그린 `#4ade80`. **영상 소스 슬라이드는 라이트 고정이므로 이 토큰을 쓰지 않는다.**

### 2-3. 영상 소스 모드 (16:9 / 9:16 슬라이드 — 흰 배경 고정, 4색 원칙)

| 용도 | 값 | 비고 |
|------|------|------|
| 배경 | `#ffffff` | **흰색 고정** (다크/검정 배경 금지) |
| 기본 텍스트/선/도형 (잉크) | `#141413` | |
| 보조 라벨 | `#5e5d59` | 메타 정보만 `#a39e98` 허용 |
| **주 강조** (핵심/데이터/현재/CTA) | `#F59E0B` | 큰 그래픽·큰 숫자. **작은 텍스트 강조는 `#D97706`** (흰 바탕 가독성) |
| **보조 강조** (성공/체크/2번째 시리즈) | `#16a34a` | |
| 빈 트랙 (도넛/바 배경) | `#efeeea` | |
| 가이드선/보더 | `rgba(20,20,19,0.08)` ~ `0.14` | 선 두께는 최소 3px 유지 |

```js
// 영상 소스 표준 색상 상수 (HTML/CSS/SVG 공용 — design.md SSOT)
const COLORS = {
  bg: '#ffffff', surface: '#f7f6f4', track: '#efeeea',
  ink: '#141413', inkSub: '#5e5d59', meta: '#a39e98',
  accent: '#F59E0B',      // 주 강조 (그래픽·큰 숫자)
  accentText: '#D97706',  // 주 강조 (작은 텍스트)
  green: '#16a34a',       // 보조 강조 (성공/체크/2번째 시리즈)
  border: 'rgba(20,20,19,0.08)', borderStrong: 'rgba(20,20,19,0.14)',
};
```

**색 역할 분담**:
- 핵심/브랜드/데이터 강조/현재 단계 = **앰버**
- 긍정/성공/완료/체크/After, A vs B 비교의 **두 번째 시리즈** = **그린**
- 중립/대조 대상/나쁜 쪽 = **잉크·뉴트럴** (색으로 부정을 표현하지 않는다 — ×마커·텍스트 맥락으로)
- **레드는 영상 소스에서 사용 금지** (Before/After의 Before = 뉴트럴 + `×` 잉크 마커, After = 그린 + `✓`)

### 2-4. 썸네일 모드

| 항목 | 권장 |
|------|------|
| 기본 조합 | 어두운 배경(검정/딥브라운) + **앰버/옐로(#FBBF24) 강조** + 흰색 텍스트 — "검정+노랑" 고대비는 클릭 유도 검증 조합 |
| 보조 조합 | 웜 아이보리(#f7f6f4) 배경 + 잉크 텍스트 + 앰버 포인트 — 영상 소스(흰 배경)와 톤을 맞추고 싶을 때 |
| 인물/사진 | 자유 (실사진 색감은 제약 없음) — 텍스트/그래픽 요소만 브랜드 색 |
| 예외 | CTR이 우선인 매체이므로, 근거가 있으면 브랜드 색 이탈 허용. 단 기본값은 브랜드 모드 |

---

## 3. 타이포그래피

| 항목 | 값 |
|------|------|
| 본문/제목 폰트 | `'Pretendard', 'Noto Sans KR', system-ui, -apple-system, sans-serif` |
| 코드/숫자 폰트 | `'JetBrains Mono', 'Fira Code', 'Consolas', monospace` |
| 인용부호 장식 | `Georgia, serif` (따옴표 마크 한정) |
| 숫자 표기 | `font-variant-numeric: tabular-nums` (카운터/KPI 필수) |

**웨이트 시스템**: 900(히어로) / 800(헤딩·KPI) / 700(강조·라벨) / 600(서브) / 500(본문·태그)

**자간 규칙**:
- 큰 글자일수록 타이트하게: 히어로 `-0.045em`, h1 `-0.03em`, h2 `-0.02em`
- 섹션 라벨(소형 대문자): `letter-spacing 0.1~0.3em` + `uppercase`
- 영상 텍스트는 한글 우선 (고유명사만 영어) — 라벨도 한글

**타입 스케일 (clamp 토큰)**:

```css
--text-xs:   clamp(0.70rem, 0.4vw + 0.63rem, 0.85rem);
--text-sm:   clamp(0.85rem, 0.5vw + 0.75rem, 1.00rem);
--text-base: clamp(1.00rem, 0.55vw + 0.87rem, 1.13rem);
--text-lg:   clamp(1.13rem, 0.7vw + 0.97rem, 1.38rem);
--text-xl:   clamp(1.38rem, 1vw + 1.15rem, 1.75rem);
--text-2xl:  clamp(1.75rem, 2vw + 1.30rem, 2.50rem);
--text-3xl:  clamp(2.25rem, 3vw + 1.50rem, 3.50rem);
--text-4xl:  clamp(3.00rem, 5vw + 1.50rem, 5.50rem);
--text-hero: clamp(3.5rem, 8vw + 1rem, 7.5rem);
```

(영상 화면 기준 px 글자 크기 표는 각 스킬 문서를 따른다 — 16:9는 youtube-editor, 9:16은 youtube-short-generator.)

---

## 4. 모션 토큰

| 토큰 | 값 |
|------|------|
| 기본 전환 시간 | `0.45s` |
| 기본 이징 | `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (ease-out 계열) |
| 스태거 딜레이 | `80ms` (요소당 `--i` 인덱스 × 80ms) |
| 등장 이동 거리 | `translateY(16~28px)` (레이아웃 시프트 금지) |

**모션 렉시콘 (2026-07-31 확장)** — 모션 지시·편집지시서·기획메모는 아래 **용어**로만 기술한다. 각 용어는 보일러플레이트의 구현 클래스와 1:1 매핑이다(용어 따로 구현 따로 금지 — 렉시콘에 없는 연출이 필요하면 preset으로 등재한 뒤 쓴다):

| 용어 (한/영) | 구현 | 정의 | 용도 | 기본 시간 |
|------|------|------|------|------|
| 스태거 (stagger) | `.el` + `--i` | `translateY(16px)` → 원위치, 요소당 +80ms 순차 | 모든 요소 기본 등장 | 0.4s |
| 라이즈 (rise) | `.el--rise` | `translateY(28px) scale(0.96)` → 원위치 | 카드/이미지 등장 | 0.6s |
| 팝 (pop) | `.el--pop` | `scale(0.88)` → `1.04` 오버슈트 → `1` | 로고/아이콘/**스티커** 등장 (바운스) | 0.55s |
| 그로우 스케일 (grow-scale) | `.grow-scale` | `scale(0.3)` → `1.08` → `1` | 강조 요소 임팩트 등장 | 0.85s |
| 펄스 (pulse) | `.pulse` | 앰버 링이 0→14px 퍼짐, 반복 | 핵심 요소 시선 유도 | 2s 반복 |
| 그로우 인 (grow-in) | `.hbar-fill` / `.donut-arc` | 바 width·도넛 arc가 **0→값**으로 그려짐 | 데이터 최초 등장 | 1~1.2s |
| 카운터 (number counter) | `[data-counter]` | 숫자가 0(또는 `data-from`)→목표값으로 카운팅 | KPI/빅 넘버 | 1.1s |
| 프로그레스 스윕 (progress sweep) | `.hbar-fill--sweep` / `.donut-arc--sweep` + 카운터 `data-from` | 이미 그려진 바/도넛/숫자가 **값 A→B**로 차오름 (grow-in과 구분: 0→값이 아님) | Before→After 수치 변화, 게이지 대체 | 1s |
| 하이라이트 스윕 (highlight sweep) | `.hl-sweep` | 앰버 형광펜 띠가 텍스트 뒤를 좌→우로 훑고 지나감 | 핵심 문구 강조 | 0.6s |
| 드로우온 (draw-on) | `.u-draw`(밑줄) / `.strike-draw`(취소선) / `.draw-on`(SVG stroke) | 밑줄·취소선·선이 좌→우로 그어짐 | 강조 밑줄, "아니다" 취소선, 다이어그램 선 | 0.6~0.7s |
| 와이프 리빌 (wipe reveal) | `.wipe-reveal` | 마스크(clip-path)가 지나가며 요소가 드러남 | 타이틀/이미지 극적 등장 (Lottie y1의 CSS 범용판) | 0.65s |
| 크로스 디졸브 (cross dissolve) | 슬라이드 전환 기본값 | 앞 슬라이드가 겹쳐 사라지며 다음이 등장 | 모든 슬라이드 전환 (별도 지시 불필요) | 0.45s |
| 키네틱 타이포그래피 (kinetic typography) | `slam`(9:16) / `grow-scale`+히어로 조합 | 글자 자체가 움직이는 연출 | 🔒 **게이트: 히어로/훅 슬라이드 한정, 영상당 1~2회** — 미니멀 무드 오염 방지 | 0.35~0.85s |
| ⏸ 화이트보드 드로잉 (whiteboard draw) — **비활성(2026-08-16 오너 결정, 제안·사용 금지. 켜는 법 = `scripts/whiteboard/README.md`)** | `scripts/render_whiteboard.py` → 오버레이 `wb<n>.mp4`(`style: "whiteboard"`) | 흰 화면 위에 손+마커가 4색 선화를 **순서대로 그려 나감**(구역별 잉크→채색). 슬라이드 위 오버레이 트랙에 얹힘 | 스토리·비유·개념 설명 등 **수치 없는 대목**. 🔒 **게이트: 영상당 1~3장면, 한 장면 8~30초, 데이터·비교·프로세스에는 쓰지 않음**(그건 차트·카드가 더 낫다). 장면 규칙 = `템플릿/whiteboard/README.md` (2026-08-16 신설, 실측 판단 전 — 별로면 `scripts/whiteboard/README.md` 롤백) | 구간 길이 그대로 |
| 콜라주 인서트 (paper cutout collage) | `scripts/render_collage.py` → 오버레이 `ov<n>.mp4`(`style: "collage"`, 대본 = `04_영상소스/collage/cg<n>.json`) | 색종이 배경 위에 오려낸 종이 조각이 하나씩 "탁" 붙는 스톱모션 콜라주(찢은 가장자리·하드 섀도·posterize 8fps boil). 풀스크린 인서트로 슬라이드를 대체 | **🔒 오프닝(t=0) 필수 — 영상의 첫 장면은 반드시 콜라주로 연다(2026-08-18 오너 결정: 시청 시작 후킹·몰입)**. 본문은 실패담·에피소드 서사, 항목 나열·비교(2~3개), 수치·비율(종이 파이·코인 막대). 🔒 **게이트: 편당 2~3개 = 오프닝 1(5~20초) + 본문 1~2(15~45초), 합계 ≤ 러닝타임 25%(스크립트 강제: 오프닝 부재·4개 이상·25% 초과 중단). 화면 시연·코드 설명·추상 개념에는 쓰지 않음. 전 구간 콜라주 금지(2026-08-17 오너 비교 후 확정)** — 규칙·스키마 = youtube-editor SKILL.md Step 6.7 + `공유 프로젝트/cutout-collage-lab/README.md` | 구간 길이 그대로 |

- 모든 등장은 **순차(스태거)**, 동시 등장 금지.
- scale 바운스는 0.88~1.08 범위.
- 카운터 숫자는 JS 헬퍼(`animateCounter`)로 슬라이드 활성화 시 카운팅. `data-from`으로 시작값 지정 시 프로그레스 스윕이 된다.
- 스윕·드로우온·리빌류는 **한 슬라이드에 1종만** — 강조 preset이 겹치면 강조가 아니다. 기본 등장(스태거/라이즈/팝)과의 조합은 자유.

**장면 문법 — preset 조합 원칙 (2026-07-23 신설)**: 장면의 모션 연출은 매번 새 애니메이션 코드를 짜는 게 아니라 「**preset(위 렉시콘 용어 또는 Lottie 씬) + duration + params(대상·수치) + transition(등장 방식) + camera(줌/확대 유무)**」 5개 값의 조합으로 기술한다. 자유도를 줄일수록 영상 전체 톤이 일관해진다. 새 안무가 필요해서 만들었는데 다음 영상에서도 쓸 것 같으면, 1회용 인라인 코드로 두지 말고 위 표(또는 `템플릿/lottie/`)에 preset으로 등재한 뒤 쓴다. (출처: Remotion 프로모션 영상 제작기(스레드 @minhyeok.seo, 2026-07)의 "자유도를 줄인 게 오히려 도움" 원칙 — 기존 시그니처 패턴 표와 같은 철학의 명문화)

**Lottie 모션 씬 (2026-07-06 신설)**: 복잡한 안무(마스크 리빌·마커 스윕·카운트업 베이크·링 드로우 + 체크 팝)는
`템플릿/lottie/`의 Lottie JSON 씬을 `.lottie-stage`로 드롭해 재사용한다 — 슬라이드 활성화 시 0프레임부터 재생되어
캡처 타임라인과 결정론 동기. 씬 색·폰트도 이 파일(4색 원칙·Pretendard 800/900)이 SSOT.
사용법·주의(빈 `chars` 키 금지)는 `템플릿/lottie/README.md`.

**렌즈 후처리 레이어 (선택 연출 — 2026-07-23 신설, 기본 미사용)**: 3D 원근(perspective)이나 큰 확대(줌) 연출이 있는 장면에 한해, 화면 위에 후처리 레이어 1장을 얹어 공간감을 더할 수 있다.
- 방식: radial mask로 **중앙은 선명하게 유지**하고, **가장자리에만** `blur`(≤8px)와 아주 약한 색수차(RGB 1~2px 오프셋)를 더한다. 콘텐츠 요소 자체에 blur를 걸지 않는다(가독성 소유권은 콘텐츠에 있다).
- 게이트: ① 평면 슬라이드(차트·카드·리스트)에는 쓰지 않는다 — 흰 배경 미니멀 무드에서 과한 연출은 §7 금지 무드("대시보드 느낌"류)와 같은 방향의 오염이다. ② 텍스트·자막 영역이 blur 범위에 들어가면 안 된다. ③ 한 영상에서 남발하지 않는다(강조 장면 1~2곳).
- 근거: 물리적으로 완벽한 3D보다 "UI 가독성을 유지하면서 충분한 공간감"이 목적(스레드 @minhyeok.seo Remotion 제작기, 2026-07).

---

## 5. 컴포넌트 문법 (슬라이드/인포그래픽 공용)

| 컴포넌트 | 문법 |
|----------|------|
| **섹션 라벨** | 소형 uppercase + `letter-spacing 0.2em` + `#D97706` 텍스트 + `--accent-subtle` 배경 칩 (radius 4px) |
| **카드** | `--bg-surface` 배경 + 1px `--bg-border` + radius 12~18px + 은은한 그림자. **강조 카드**는 보더 `--accent` + 앰버 링 그림자 |
| **KPI 카드** | 라벨(소형 uppercase, `#a39e98`) + 큰 숫자(900, tabular-nums, 잉크) + 단위. 강조 KPI만 숫자가 앰버 |
| **태그/칩** | radius 999px + 1px 보더. 강조 칩은 `--accent-subtle` 배경 + `#D97706` 텍스트 |
| **Before/After** | Before = 뉴트럴 카드 + `×` 잉크 마커 / After = `--green-soft` 배경 + `--green-ring` 보더 + `✓` 그린 마커 (**레드 금지**) |
| **체크 불릿** | `✓` = 그린 / `×` = 잉크 (레드 금지) |
| **숫자 리스트** | 모노 폰트 큰 숫자(800) + 텍스트. 강조 숫자 = `#D97706`, 성공 숫자 = 그린 |
| **코드 블록** | mac 신호등 점 3개 헤더(`#ff5f57` `#febc2e` `#28c840` — 장식 예외) + 언어 뱃지(`#D97706`) + `--bg-surface` 본문 |
| **인용** | 대형 Georgia 따옴표(앰버, opacity 0.35) + 이탤릭 본문 + `#D97706` 저자명. 인물 사진은 원형 + 3px 앰버 보더 |
| **브랜드 로고 칩** | 원형, 흰 배경 + 1px 보더 + elevated 그림자 |
| **점 네비/스테이지 닷** | 비활성: 작은 점, 활성: 앰버 + 가로로 늘어난 알약형 |
| **강조 마침표** | 문장 끝 `.` 또는 점을 앰버로 — 엔딩/타이틀 시그니처 |
| **스테이지 시스템** | 한 슬라이드 안에서 이미지/단계 순차 전환 (`.stage.is-active`) — 다중 Phase 연출용 |
| **스티커** | `.sticker` 래퍼 + `레퍼런스/스티커/`의 4색 벡터 SVG. 등장 = 팝, 시선 유도 = 펄스. 감정 전환/리액션 장면의 시각 주인공 |

**스티커 애셋 규칙 (2026-07-31 신설)**: 플랫 인포그래픽의 시각적 단조로움을 보완하는 재사용 일러스트 애셋. SSOT = `레퍼런스/스티커/README.md`(대장·파일명 규약).
- **벡터 4색 통일**: SVG(또는 Lottie JSON)로만 제작하고 색은 이 파일의 4색 원칙(잉크/앰버/그린 + 흰 배경·뉴트럴 음영)을 색값 수준에서 지킨다. 실사·자유색 래스터 스티커 금지 (실사 질감이 필요하면 스티커가 아니라 이미지 카드로 처리).
- **파일명이 곧 검색이다**: `<주제>-<표정·동작>-<주색>.svg` (예: `전구-반짝-앰버.svg`). 새 스티커를 만들면 반드시 이 규약으로 `레퍼런스/스티커/`에 저장하고 README 대장에 1행 추가한다 — 다음 영상에서 재제작하지 않고 재사용한다.
- **먼저 스캔, 없으면 제작**: 슬라이드에 스티커가 필요하면 `레퍼런스/스티커/`를 묻지 말고 자동 스캔해 재사용하고, 없을 때만 신규 제작 후 등재한다 (로고/썸네일 레퍼런스와 같은 원칙). 사용 시 결과물 `04_영상소스/`로 복사해 상대경로 참조.

---

## 6. 인포그래픽 브랜드 적용 규칙 (영상 소스)

영상 소스 인포그래픽(SVG/차트/다이어그램)을 만들 때:

1. **배경은 흰색** — 모든 요소는 흰 바탕에서 잘 보여야 한다. 너무 옅은 회색(`rgba(20,20,19,0.2)` 미만의 잉크) 텍스트 금지.
2. **주 강조 앰버 `#F59E0B`** — 차트 핵심 데이터, 카운터 숫자, 현재 단계. 작은 텍스트는 `#D97706`.
3. **보조 강조 그린 `#16a34a`** — 성공/체크/After, A vs B 비교의 두 번째 시리즈.
4. 대조 대상/중립은 잉크(`#141413`) 또는 뉴트럴 바(`#efeeea` + 보더).
5. 도넛/바의 빈 트랙은 `#efeeea`, 선·보더는 3px 이상 (영상 축소 시 가독성).
6. Mermaid/D2 스타일: 강조 노드 `fill:#F59E0B,stroke:#D97706,color:#141413`, 성공 노드 `fill:#16a34a,stroke:#16a34a,color:#ffffff`, 기본 노드 `fill:#f7f6f4,stroke:#141413,color:#141413`.
7. 쇼츠 자막(캡컷) 강조 색상도 앰버 계열 권장: 활성 단어 `#F59E0B`, 비활성 `#141413`.
8. 구 색상이 남아 있으면 발견 즉시 교체: `#00FF88`·`#FFD700`·`#FBBF24`(영상 소스 내) → `#F59E0B`/`#D97706`, 검정 배경(`#000000`) → 흰색(`#ffffff`).

---

## 7. 금지 사항 (영상 소스/인포그래픽)

```
❌ 검정/다크 배경 (영상 소스는 흰색 고정 — 썸네일만 예외)
❌ 4색 외 색 사용 (보라/파랑/네온그린/골드/레드 등)
❌ 레드로 부정 표현 (Before·나쁜 쪽 = 뉴트럴 + ×마커)
❌ 한 화면에 강조색 역할 혼동 (앰버=핵심, 그린=긍정/2번째 — 역할 바꿔 쓰지 않기)
❌ 보라색/무지개 그라데이션
❌ 너무 옅은 텍스트 (#a39e98는 메타 한정, 그보다 옅은 회색 금지)
❌ 작은 텍스트에 #F59E0B (가독성 — #D97706 사용)
❌ 게이지/스피드미터(반원형) — 대시보드 느낌, 도넛/바로 대체
❌ 이모지 파티클, 둥근 박스 남발, 얇은 보더 박스 나열
❌ Tailwind 색상 클래스 (text-yellow-400 등) — 항상 hex 직접 지정
```

---

## 8. 매체별 참조 맵

| 산출물 | 모드 | 상세 규칙 | 보일러플레이트 |
|--------|------|------|------|
| 16:9 영상 소스 슬라이드 | 영상 소스 모드 (§2-3) | `.claude/skills/youtube-editor/` | `템플릿/presentation-16x9.html` |
| 9:16 쇼츠 소스 슬라이드 | 영상 소스 모드 (§2-3) | `.claude/skills/youtube-short-generator/` | `템플릿/presentation-9x16.html` |
| 썸네일 | 썸네일 모드 (§2-4) | youtube-editor `SKILL.md` Step 9-2 (배경 이미지 프롬프트) | — |
| 화이트보드 드로잉 장면 (16:9 오버레이) | 영상 소스 모드 (§2-3) — 흰 배경·4색 선화 | youtube-editor `SKILL.md` Step 6.6 + `템플릿/whiteboard/README.md` | `템플릿/whiteboard/scene-example.svg` |
| 프레젠테이션(영상 외 용도) | 라이트 기본 + 다크 토글 (§2-1, §2-2) | 원본 레퍼런스 HTML 3종 | — |
